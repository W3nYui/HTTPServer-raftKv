# Raft Gomoku

一个在线五子棋应用，以 Raft KV 集群作为 PVP 对局状态的一致性和持久化来源。它将用户交互、实时连接和分布式状态复制明确分层，便于演示和解释。

## Language

**应用服务器**:
承载 HTTP 和 WebSocket 接口、执行鉴权并协调对局命令的进程。它通过 Raft 客户端访问共享的对局状态。
_Avoid_: 前端服务器, Raft 节点

**Raft 集群**:
由多个 KV 节点组成、通过日志复制达成一致的状态存储集群。它是 PVP 对局状态的唯一权威来源。
_Avoid_: 数据库, 后端服务

**对局状态**:
一盘 PVP 五子棋的参与者、棋盘、当前回合、落子数和结束结果的完整快照。对局状态的每一次有效变更都必须先由 Raft 集群提交。
_Avoid_: 房间缓存, 前端状态

**PVP 对局**:
两名已登录玩家之间的一盘五子棋。第一阶段中，它是唯一使用 Raft 集群复制状态的游戏模式。
_Avoid_: AI 对局, 本地对局

**落子命令**:
玩家在指定对局坐标落下一子的请求，携带玩家身份与防重复标识。应用服务器只在 Raft 提交后的对局状态确认该请求的结果。
_Avoid_: 前端落子, 本地写入

**已提交状态**:
已由 Raft 集群多数副本确认并可被恢复的对局状态。只有已提交状态可以被广播为有效落子结果。
_Avoid_: 乐观状态, 本地暂存状态

**同步失败**:
应用服务器无法在限定时间内将落子命令提交到 Raft 集群的结果。客户端必须先请求最新已提交状态，不能自动重放原落子。
_Avoid_: 本地成功, 自动重试落子

**活跃对局**:
尚未结束、并允许应用服务器从 Raft 恢复后继续的 PVP 对局。第一阶段仅为活跃对局建立恢复索引。
_Avoid_: 对局历史, 回放记录

**对局快照**:
以房间 ID 标识、包含完整对局状态的 JSON 值。每次有效落子都会替换该房间的已提交快照。
_Avoid_: 落子事件流, 增量补丁
